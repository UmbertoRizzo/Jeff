// Punto di integrazione del futuro crawler e dell'indice dei contenuti.
export async function getSiteContext({ siteId, question, pageUrl }) {
    void siteId;
    void question;
    void pageUrl;

    return "";
}
